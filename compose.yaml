services:
  app:
    build:
      context: .
    image: annotation-desk
    ports:
      - "8000:8000"
    environment:
      PORT: "8000"
      ANNOTATE_TOOL_DATA_DIR: /data
    volumes:
      - ./annotation-data:/data
    restart: unless-stopped
